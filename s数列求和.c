#include<stdio.h>
#include<math.h>
int main()
{
    int m,i;
    double sum,n;
    while(scanf("%lf%d",&n&m)!=EOF)
    {
        sum=0.0
        while(i<=m)
        {
            i++;
            n=sqrt(n);
            sum=sum+n;
            printf("%.2f",sum);
            }
    }
}
